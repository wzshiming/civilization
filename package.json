{
  "name": "civilization",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "shared",
    "backend",
    "map-generator-cli",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev:generator && concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "MAPS_DIR=../maps/ npm run dev -w backend",
    "dev:generator": "npm run dev -w map-generator-cli -- -d ../maps/",
    "build:frontend": "npm run build -w frontend",
    "build:backend": "npm run build -w backend",
    "build:generator": "npm run build -w map-generator-cli",
    "build:shared": "npm run build -w shared",
    "build": "npm run build:shared && npm run build:backend && npm run build:generator && npm run build:frontend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "preview": "npm run preview -w frontend",
    "postinstall": "npm run build:shared"
  },
  "dependencies": {
    "@civilization/shared": "file:./shared",
    "d3-delaunay": "^6.0.4"
  },
  "devDependencies": {
    "@types/d3-delaunay": "^6.0.4",
    "@types/node": "^24.10.1",
    "typescript": "~5.9.3",
    "tsx": "^4.7.0"
  }
}
